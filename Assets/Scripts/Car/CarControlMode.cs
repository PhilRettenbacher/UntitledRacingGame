public enum CarControlMode
{
    Enabled, //Full Control
    Disabled, //No Control, Physics still apply
    Static //No Control, no Physics
}

public enum DriveMode
{
    FrontWheelDrive,
    RearWheelDrive,
    AllWheelDrive
}
